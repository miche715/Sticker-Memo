import React from 'react';

const style = {
    wrapper: {
        width: "50%", 
        height: "100%", 

        background: "blue"
    },
}

const MapView = (props) => {
    return (
        <div style={style.wrapper}>MapView</div>
    );
};

export default MapView;